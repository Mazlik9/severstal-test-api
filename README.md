# Документация для локальной развертки бэкенда

### 1. Клонирование репозитория
```bash
# Клонируйте репозиторий
git clone <ссылка-на-репозиторий>
cd <название-папки-проекта>
```

### 2. Проверка окружения
Убедитесь, что в корне проекта есть файл `.env` со следующим содержимым:
```
AUDIENCE=None
ISSUER=None
JWT_PRIVATE_KEY=private_key.pem
JWT_PUBLIC_KEY=public_key.pem

# Django
SECRET_KEY=your-super-secret-key-here
ENVIRONMENT=development
DEBUG=True

# Database
DB_NAME=buildhub_dev
DB_USER=developer
DB_PASSWORD=dev_password
DB_HOST=db
DB_PORT=5432

# CORS
ALLOWED_HOSTS=localhost,127.0.0.1,backend
```

### 3. Запуск проекта
```bash
# Запустите проект (логи будут выводиться в консоль)
docker-compose -f docker-compose-local.yml up

# Или запустите в фоновом режиме
docker-compose -f docker-compose-local.yml up -d
```

### 4. Проверка работы
После запуска проверьте доступность:
- **Бэкенд**: http://localhost:8000
- **Админка Django**: http://localhost:8000/admin
- **API документация**: http://localhost:8000/api/docs

## 🔧 Основные команды

### Управление контейнерами
| Команда | Описание |
|---------|----------|
| `docker-compose -f docker-compose-local.yml up` | Запустить все сервисы |
| `docker-compose -f docker-compose-local.yml up -d` | Запустить в фоновом режиме |
| `docker-compose -f docker-compose-local.yml down` | Остановить все сервисы |
| `docker-compose -f docker-compose-local.yml down -v` | Остановить и удалить тома БД ⚠️ |
| `docker-compose -f docker-compose-local.yml restart` | Перезапустить сервисы |
| `docker-compose -f docker-compose-local.yml ps` | Показать статус контейнеров |

### Просмотр логов
| Команда | Описание |
|---------|----------|
| `docker-compose -f docker-compose-local.yml logs` | Показать все логи |
| `docker-compose -f docker-compose-local.yml logs -f` | Показать логи в реальном времени |
| `docker-compose -f docker-compose-local.yml logs backend` | Логи только бэкенда |
| `docker-compose -f docker-compose-local.yml logs db` | Логи только базы данных |

### Работа с базой данных
```bash
# Подключиться к PostgreSQL
docker-compose -f docker-compose-local.yml exec db psql -U developer -d buildhub_dev

# Создать дамп базы данных
docker-compose -f docker-compose-local.yml exec db pg_dump -U developer buildhub_dev > backup.sql

# Восстановить из дампа
cat backup.sql | docker-compose -f docker-compose-local.yml exec -T db psql -U developer buildhub_dev
```

## 👑 Создание суперпользователя Django

### Что такое суперпользователь?
Суперпользователь (superuser) - это администраторская учётная запись в Django, которая имеет полный доступ к:
- Админ-панели Django (`/admin`)
- Всем моделям базы данных
- Управлению пользователями и правами
- Настройкам приложения

### Как создать суперпользователя

1. **Убедитесь, что контейнеры запущены**
```bash
docker-compose -f docker-compose-local.yml ps
```

2. **Запустите команду создания суперпользователя**
```bash
docker-compose -f docker-compose-local.yml exec backend python manage.py createsuperuser
```

3. **Следуйте инструкциям в терминале:**
```
Email address: admin@example.com  # Введите email (можно пропустить)
Password: ********  # Введите пароль (минимум 8 символов)
Password (again): ********  # Повторите пароль
Superuser created successfully.
```

### Пример использования суперпользователя

1. **Войдите в админ-панель:**
   - Откройте http://localhost:8000/admin
   - Введите созданные логин и пароль

2. **Что можно делать в админке:**
   - Управлять пользователями и группами
   - Просматривать и редактировать данные моделей
   - Настраивать разрешения
   - Управлять сессиями

### Если забыли пароль
```bash
# Сбросить пароль суперпользователя
docker-compose -f docker-compose-local.yml exec backend python manage.py changepassword admin
```

## 🗄️ Управление данными

### Миграции базы данных
```bash
# Применить миграции к базе данных
docker-compose -f docker-compose-local.yml exec backend python manage.py migrate
```
**Для чего это нужно?**
При обновлении таблиц баз данных в коде эти обновления нужно применить для уже развернутой бд,
чтобы не делать это SQL-скриптами в Django придумали миграции, которые делают это за тебя.


## 🔍 Отладка и диагностика

### Доступ к контейнерам
```bash
# Открыть терминал внутри контейнера бэкенда
docker-compose -f docker-compose-local.yml exec backend bash

# Открыть терминал внутри контейнера базы данных
docker-compose -f docker-compose-local.yml exec db sh

# Выполнить Django shell
docker-compose -f docker-compose-local.yml exec backend python manage.py shell
```

### Проверка здоровья
```bash
# Проверить доступность бэкенда
curl http://localhost:8000/health/

# Проверить подключение к БД
docker-compose -f docker-compose-local.yml exec db pg_isready -U developer -d buildhub_dev

# Проверить переменные окружения
docker-compose -f docker-compose-local.yml exec backend env | grep DB_
```

## 🧹 Очистка и обслуживание

### Регулярная очистка
```bash
# Остановить и удалить контейнеры
docker-compose -f docker-compose-local.yml down

# Очистить Docker кэш
docker system prune -f

# Удалить неиспользуемые образы
docker image prune -a
```

### Полный сброс
```bash
# Остановить, удалить контейнеры и тома БД. НЕ ДЕЛАТЬ ЕСЛИ ЖАЛКО БАЗУ ДАННЫХ!!!
docker-compose -f docker-compose-local.yml down -v

# Удалить сгенерированные JWT ключи
rm -f private_key.pem public_key.pem

# Пересобрать с нуля
docker-compose -f docker-compose-local.yml build --no-cache
docker-compose -f docker-compose-local.yml up
```

## ⚠️ Распространённые проблемы и решения

### 1. Порт уже используется
```
Error: Port 8000 is already in use
```
**Решение:**
```bash
# Найдите процесс, использующий порт
lsof -i :8000
# Завершите процесс или измените порт в docker-compose
```

### 2. Ошибка подключения к базе данных
```
django.db.utils.OperationalError: could not connect to server
```
**Решение:**
```bash
# Убедитесь, что БД запущена
docker-compose -f docker-compose-local.yml ps

# Перезапустите БД
docker-compose -f docker-compose-local.yml restart db

# Проверьте логи БД
docker-compose -f docker-compose-local.yml logs db
```

### 3. Ошибка прав доступа
```
Permission denied
```
**Решение:**
```bash
# Пересоберите образы
docker-compose -f docker-compose-local.yml build --no-cache
```

### 4. Контейнеры не запускаются
```
Exited with code 1
```
**Решение:**
```bash
# Просмотрите логи
docker-compose -f docker-compose-local.yml logs

# Проверьте файл .env
cat .env

# Попробуйте запустить пошагово
docker-compose -f docker-compose-local.yml up db
docker-compose -f docker-compose-local.yml up backend
```

## 📚 Полезные команды для разработки

### Часто используемые команды
```bash
# Перезапустить только бэкенд
docker-compose -f docker-compose-local.yml restart backend

# Показать использование ресурсов
docker stats

# Показать все Docker образы
docker images

# Удалить все остановленные контейнеры
docker container prune
```

## 🔒 Безопасность

### Важные моменты:
1. **JWT ключи** генерируются автоматически при каждом запуске
2. **Пароль БД** хранится в `.env` файле (не коммитьте его в Git!)
3. **DEBUG режим** включен только для локальной разработки
4. **Суперпользователь** создается только для админки


## 🤝 Интеграция с фронтендом

### Настройка CORS
Бэкенд уже настроен для работы с фронтендом на:
- `localhost` (любой порт)
- `127.0.0.1`
- `backend` (имя сервиса в Docker)

### Пример .env для фронтенда
```bash
# В проекте фронтенда
REACT_APP_API_URL=http://localhost:8000/api/
REACT_APP_WS_URL=ws://localhost:8000/ws/
```

---
